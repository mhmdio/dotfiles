# nix build output symlinks
result
result-*

# macOS
.DS_Store

# direnv
.direnv/

# secrets: encrypted files under secrets/ are meant to be committed — these never are
keys.txt
*.agekey
secrets/*.dec.*
